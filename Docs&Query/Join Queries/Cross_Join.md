### **Cross JOIN**: The CROSS JOIN keyword returns the Cartesian product of both tables, i.e., all possible combinations of rows from both tables.
* `SELECT` column_names `FROM` table1 T1, `Cross join` table2 T2;
<a name="view"></a>
